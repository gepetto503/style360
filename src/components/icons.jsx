import React from 'react';
import AppointmentsIcon from './appointments-icon';

const Icons = (props) => {
  return (
    <div className="icons-wrapper">
      <div className="icon">
        <AppointmentsIcon></AppointmentsIcon>
      </div>
      <div className="icon">
        <AppointmentsIcon></AppointmentsIcon>
      </div>
      <div className="icon">
        <AppointmentsIcon></AppointmentsIcon>
      </div>
    </div>
  )
}

export default Icons;
